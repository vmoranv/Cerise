{
  "$schema": "https://cerise.dev/schemas/plugin-manifest-v2.json",
  "name": "echo-python",
  "version": "0.1.0",
  "display_name": "Echo (Python)",
  "description": "Minimal stdio JSON-RPC plugin implemented in Python.",
  "author": "Cerise",

  "runtime": {
    "language": "python",
    "entry": "main.py",
    "transport": "stdio"
  },

  "abilities": [
    {
      "name": "echo_python",
      "description": "Echo text back (optionally with a prefix from config).",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "Text to echo"
          }
        },
        "required": ["text"]
      }
    }
  ],

  "permissions": [],

  "config_schema": {
    "type": "object",
    "properties": {
      "prefix": {
        "type": "string",
        "default": "",
        "description": "Optional prefix added to echoed text"
      }
    }
  }
}
